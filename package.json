{
  "name": "tasuke",
  "version": "1.0.0",
  "license": "MIT-2.0",
  "scripts": {
    "format": "biome format --write . && biome lint --apply-unsafe . && prettier --write ."
  },
  "engines": {
    "node": ">=22.0"
  },
  "packageManager": "pnpm@9.14.2",
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "prettier": "^3.4.1"
  }
}
